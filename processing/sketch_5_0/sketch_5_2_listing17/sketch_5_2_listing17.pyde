def setup():
    size(500, 500)
    smooth()
    background(255)
    strokeWeight(30)
    noLoop()
def draw():
    stroke(20)
    i=int()
    n=[(i=1), (i<8), (i=i+1)]
    for i in range (n)
    line(i*50, 200, 150 + (i-1)*50, 300)
